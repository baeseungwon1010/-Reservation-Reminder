# 배포 기록

> 배포본 파일명은 항상 **`예약알리미.apk`** 로 고정합니다.
> 아래 "현재 최신 버전"이 그 파일이 담고 있는 버전입니다.

## 현재 최신 버전

- **v1.0.1 (빌드 2)** — 2026-08-19
- 파일: `예약알리미.apk`

## 버전 이력

| 버전 | 빌드 | 날짜 | 요약 |
|---|---|---|---|
| v1.0.1 | 2 | 2026-08-19 | 버전 관리 도입(빌드마다 증가), 앱에 실제 버전 표시, 배포본 이름 고정 |
| v1.0.0 | 1 | 2026-08-17 | 최초 배포~피드백 반영(달력 마커·아이콘·문구·오류 통일 등) |

---

## 배포 방법

프로젝트 루트에서:

```bash
bash scripts/release.sh          # 빌드번호 자동 +1 후 빌드·배포
bash scripts/release.sh 1.1.0    # 사용자용 버전까지 지정(1.1.0) + 빌드번호 +1
```

스크립트가 하는 일:
1. `pubspec.yaml` 의 버전을 올림 (빌드번호 +1, 인자 주면 사용자 버전도 변경)
2. `flutter build apk --release`
3. 산출물을 **`예약알리미.apk`** 로 복사
4. 이 파일(`RELEASE.md`)의 "현재 최신 버전"과 이력 표를 갱신
