# 모바일 시설 예약 알림 앱 개발 계획서

## 1. 프로젝트 개요

### 1.1 목적

강당·강의실 등의 시설 사용 날짜를 등록하면 해당 시설의 예약 규칙에 따라 예약해야 하는 날짜를 자동으로 계산하고, 해당 날짜에 모바일 알림을 제공하는 개인용 앱을 개발한다.

이 앱은 예약 시스템 자체를 대체하지 않으며, 외부 예약 시스템과 연동하지 않는다.

### 1.2 핵심 동작

```text
시설 선택
  ↓
사용 날짜 선택
  ↓
시설별 예약 규칙 적용
  ↓
예약 스케줄 자동 생성
  ↓
로컬 알림 등록
  ↓
예약일에 알림
```

### 1.3 사용 목적

- 개인 사용
- 상용화 목적 없음
- 서버/계정/외부 서비스 연동 없음
- Android 및 iOS에서 독립 실행

---

## 2. 플랫폼 및 배포

### 2.1 지원 플랫폼

- Android
- iOS

Flutter 하나의 코드베이스로 양 플랫폼을 지원한다.

### 2.2 설치 형태

Android:
- Release APK 생성
- APK 파일을 통한 독립 설치 가능

iOS:
- Release 빌드
- IPA/TestFlight 등 Apple의 배포 방식 사용

---

## 3. 기술 스택

| 영역 | 기술 |
|---|---|
| 앱 프레임워크 | Flutter |
| 언어 | Dart |
| 상태 관리 | Riverpod |
| 로컬 데이터베이스 | SQLite + Drift |
| 로컬 알림 | flutter_local_notifications |
| 날짜/시간 | Dart DateTime + timezone |
| 달력 | Flutter Calendar, 필요 시 table_calendar |
| Backend | 없음 |
| API | 없음 |
| 로그인/계정 | 없음 |
| Cloud | 없음 |

### 3.1 Local-first 구조

모든 핵심 데이터는 기기 내부에 저장한다.

```text
Flutter App
├── SQLite
├── 시설 정보
├── 예약 규칙
├── 사용 일정
├── 생성 스케줄
└── 알림 설정
```

서버는 사용하지 않는다.

---

## 4. 핵심 기능

### 4.1 시설 관리

초기 설치 시 다음 시설을 기본 제공한다.

- 강당
- 강의실

각 시설은 독립적인 예약 규칙을 가진다.

예:

```text
강당
├── 7일 전 → 예약
└── 3일 전 → 예약 확인

강의실
└── 3일 전 → 예약
```

기본 규칙의 실제 일수와 내용은 실제 강당/강의실 예약 규칙에 맞춰 설정한다.

시설은 이후 추가/수정/비활성화할 수 있도록 확장 가능한 구조로 설계한다.

### 4.2 시설 비활성화

시설을 삭제할 때 기존 예약 데이터를 삭제하지 않는다.

```text
Facility
├── name
└── enabled = false
```

형태로 비활성화한다.

비활성 시설:
- 기존 예약 유지
- 기존 스케줄 유지
- 기존 알림 유지
- 신규 일정 등록 시 선택 불가

이를 통해 기존 데이터의 손실을 방지한다.

---

## 5. 달력 UI

앱의 메인 화면은 월간 달력을 중심으로 구성한다.

사용자는 달력에서 날짜를 선택하여 해당 날짜의 일정을 확인하거나 새로운 사용 일정을 등록한다.

### 5.1 월간 달력 표시

각 날짜 칸에는 일정 정보를 간략하게 표시한다.

예:

```text
┌─────────────────────────────┐
│ 일  월  화  수  목  금  토   │
├─────────────────────────────┤
│     1   2   3   4   5        │
│         🟦 강당              │
│  6   7   8   9  10  11  12  │
│     🟩 강의실                │
│         🟦 강당              │
└─────────────────────────────┘
```

시설별로 색상을 구분한다.

예:
- 강당 → 기본 색상
- 강의실 → 다른 기본 색상

색상은 시설 데이터에 저장할 수 있도록 설계한다.

### 5.2 달력에 표시하는 정보

월간 달력에서는 실제 사용 일정을 중심으로 표시한다.

예약 스케줄을 모두 달력에 표시하지 않고, 날짜를 선택하면 상세 화면에서 해당 일정의 예약 스케줄을 확인한다.

일정이 많은 날짜는 최대한 간략하게 표시하며, 세부 일정은 날짜 상세 화면에서 확인한다.

### 5.3 반응형

접근성 기능은 현재 범위에서 별도로 구현하지 않는다.

다만 Android/iOS의 다양한 화면 크기에서 UI가 깨지지 않도록 반응형으로 설계한다.

---

## 6. 사용 일정과 예약 스케줄

사용 일정과 실제 예약해야 하는 스케줄을 분리한다.

예:

```text
Reservation
├── 시설: 강당
├── 사용일: 2026-09-20
└── Schedule
    ├── 2026-09-13 예약
    ├── 2026-09-17 예약 확인
    └── 2026-09-19 준비물 확인
```

하나의 사용 일정에 여러 개의 스케줄이 연결될 수 있다.

---

## 7. 일정 등록

### 7.1 단일 일정

사용자가 다음 정보를 입력한다.

1. 시설 선택
2. 달력에서 사용 날짜 선택
3. 시설의 예약 규칙 확인
4. 자동 생성될 스케줄 확인
5. 필요하면 추가 스케줄 입력
6. 등록

예:

```text
시설: 강당
사용일: 2026-09-20

예약 규칙:
- 7일 전 → 예약
- 3일 전 → 예약 확인

생성:
- 2026-09-13 00:00 → 예약
- 2026-09-17 00:00 → 예약 확인
```

---

## 8. 예약 규칙

### 8.1 상대 날짜 규칙

MVP의 핵심 규칙은 사용일 기준 상대 날짜 계산이다.

예:

- 7일 전
- 3일 전
- 1일 전

계산은 단순 날짜 연산으로 수행한다.

```text
사용일 - N일 = 예약 스케줄 날짜
```

### 8.2 예약 시간

현재 모든 자동 생성 예약 스케줄은 자정(00:00)을 사용한다.

예:

```text
사용일: 2026-09-20

7일 전:
2026-09-13 00:00

3일 전:
2026-09-17 00:00
```

현재는 예약 시간에 대한 별도 사용자 설정을 제공하지 않는다.

### 8.3 사용자 지정 추가 스케줄

N일 전 규칙으로 생성되는 일정 외에도 사용자가 직접 추가 스케줄을 만들 수 있다.

예:

```text
09/15 담당자 확인
09/19 준비물 확인
```

추가 스케줄에는 최소한 다음 정보를 둔다.

- 제목
- 날짜
- 시간
- 알림 활성화 여부

사용자 지정 스케줄의 날짜/시간은 자동 생성 규칙과 별개로 관리한다.

---

## 9. 반복 일정 생성

반복 일정은 별도의 영구적인 "반복 일정 객체"를 만드는 방식이 아니다.

반복 조건을 이용하여 여러 개의 독립적인 사용 일정을 한 번에 생성하는 기능으로 정의한다.

### 9.1 반복 방식

두 가지 방식을 지원한다.

#### N일마다

예:

```text
시작일: 2026-09-01
종료일: 2026-10-01
반복: 7일마다
```

생성:

```text
09/01
09/08
09/15
09/22
09/29
```

#### 특정 요일

예:

```text
시작일: 2026-09-01
종료일: 2026-10-01
요일: 월요일, 수요일
```

해당 기간의 월요일/수요일마다 독립적인 사용 일정을 생성한다.

### 9.2 반복 일정 생성 이후

반복으로 생성된 일정도 일반 일정과 동일하게 취급한다.

각 사용일에 시설 예약 규칙을 각각 적용한다.

```text
반복 생성
  ↓
여러 개의 Reservation 생성
  ↓
각 Reservation에 예약 규칙 적용
  ↓
각각의 Schedule 생성
```

### 9.3 반복 생성 그룹

반복으로 생성된 일정들은 공통 `batchId`를 가진다.

이를 통해:

- 개별 일정 삭제
- 반복으로 생성된 일정 전체 삭제

를 모두 지원한다.

예:

```text
Batch A
├── 09/07 강당
├── 09/14 강당
├── 09/21 강당
└── 09/28 강당
```

09/21만 삭제할 수 있으며, 필요하면 Batch A 전체를 한 번에 삭제할 수 있다.

---

## 10. 일정 상태

사용자가 직접 완료 처리하지 않는다.

현재 기기의 날짜를 기준으로 자동으로 상태를 판단한다.

```text
스케줄 날짜 >= 오늘
→ 예정

스케줄 날짜 < 오늘
→ 완료
```

따라서 별도의 사용자 완료 버튼은 제공하지 않는다.

---

## 11. 이미 지난 스케줄

일정을 생성할 때 이미 예약 날짜가 지난 스케줄도 삭제하지 않고 생성한다.

예:

```text
현재: 2026-09-15
사용일: 2026-09-20

7일 전 → 09/13
3일 전 → 09/17
```

결과:

```text
09/13 예약       완료
09/17 예약 확인  예정
09/20 사용       예정
```

### 11.1 지난 스케줄 알림

이미 지난 스케줄에는 알림을 등록하지 않는다.

### 11.2 생성 시 경고

일정 등록 과정에서 이미 지난 스케줄이 존재하면 간단한 경고 메시지를 표시한다.

예:

> 이미 지난 일정이 있습니다.  
> 9월 13일 예약 일정은 이미 지나 알림이 등록되지 않았습니다.

지난 스케줄 자체는 정상적으로 저장한다.

---

## 12. 일정 충돌 검증

동일 시설에 일정이 중복되는 경우 충돌을 검사한다.

충돌이 발견되면 사용자에게 경고하고 일정 추가를 차단한다.

예:

```text
⚠ 일정 충돌

강당에 이미 등록된 일정이 있습니다.

2026-09-20 13:00
```

검증은 UI에서만 수행하지 않고 실제 저장 직전의 서비스/도메인 계층에서도 수행한다.

```text
UI
 ↓
Schedule Service
 ↓
Conflict Validator
 ↓
Repository
 ↓
SQLite
```

반복 일정 생성에서도 생성될 각각의 일정에 대해 충돌을 검사한다.

---

## 13. 반복 일정 삭제

반복 생성으로 만들어진 일정은 두 가지 방식으로 삭제할 수 있다.

### 개별 삭제

예:

```text
09/07
09/14
09/21
09/28
```

중 09/21만 삭제.

### 전체 삭제

`batchId`를 이용하여 해당 반복 생성 그룹에 포함된 모든 Reservation을 한 번에 삭제한다.

전체 삭제 시:

```text
Reservation 삭제
 ↓
연결 Schedule 삭제
 ↓
연결 Notification 취소
```

순서로 처리한다.

---

## 14. 알림 시스템

서버 Push가 아닌 OS Local Notification을 사용한다.

```text
일정 등록
 ↓
Notification Manager
 ↓
Android/iOS OS에 알림 예약
 ↓
앱 종료
 ↓
예약 시간 도달
 ↓
알림
```

앱이 실행 중이지 않아도 예약된 알림이 동작하도록 한다.

### 14.1 알림 ID

각 Schedule에 고유한 Notification ID를 연결한다.

이를 통해:

- 알림 등록
- 알림 수정
- 알림 삭제
- 중복 알림 방지

를 수행한다.

### 14.2 일정 수정

```text
일정 수정
 ↓
기존 알림 취소
 ↓
일정 변경
 ↓
새 알림 등록
```

### 14.3 일정 삭제

```text
일정 삭제
 ↓
관련 알림 취소
 ↓
Schedule 삭제
```

### 14.4 지난 일정

지난 일정은 알림을 생성하지 않는다.

---

## 15. 알림 동기화 및 안정성

DB의 Schedule 데이터를 실제 일정의 원본으로 사용하고, OS에 등록된 알림은 그 일정으로부터 파생되는 정보로 취급한다.

앱 시작 시:

```text
앱 시작
 ↓
DB 로드
 ↓
알림 권한 확인
 ↓
미래 Schedule 조회
 ↓
Notification Sync
 ↓
앱 실행
```

필요한 알림을 생성하고 불필요한 알림을 제거할 수 있는 구조로 만든다.

이를 통해 다음 상황에 대비한다.

- 일정 수정
- 일정 삭제
- 앱 업데이트
- 알림 상태 불일치
- 기기 재부팅 후 복구 필요 상황

Android/iOS의 실제 알림 권한, 재부팅, 배터리 절전, 알림 지연/누락 등에 대한 실기기 안정성 검증은 핵심 기능 구현 이후 별도의 안정성 테스트 단계에서 진행한다.

---

## 16. 기기 시간

기기의 현재 날짜/시간을 신뢰한다.

별도의 서버 시간이나 외부 시간 API를 사용하지 않는다.

```text
Device Time
 ↓
현재 날짜 판단
 ↓
Schedule 상태 계산
 ↓
Notification 처리
```

---

## 17. 주말 및 공휴일

현재 버전에서는 처리하지 않는다.

단순히:

```text
사용일 - N일
```

의 날짜 계산만 수행한다.

주말/공휴일이면 이전/다음 영업일로 변경하는 등의 기능은 현재 범위에서 제외한다.

---

## 18. 규칙 변경

현재 버전에서는 기존 일정에 대한 규칙 변경/재계산 기능을 제공하지 않는다.

한 번 생성된 Reservation과 Schedule은 독립적인 일정 데이터로 취급한다.

---

## 19. 데이터 모델

### Facility

```text
Facility
├── id
├── name
├── color
├── enabled
├── createdAt
└── updatedAt
```

### Rule

```text
Rule
├── id
├── facilityId
├── title
├── type
├── offset
├── enabled
├── createdAt
└── updatedAt
```

### Reservation

```text
Reservation
├── id
├── facilityId
├── usageDate
├── batchId (nullable)
├── createdAt
└── updatedAt
```

### Schedule

```text
Schedule
├── id
├── reservationId
├── title
├── date
├── source
├── notificationEnabled
├── notificationId
├── createdAt
└── updatedAt
```

`batchId`는 반복 생성으로 묶인 Reservation을 식별하는 데 사용한다.

---

## 20. 아키텍처

```text
                    Flutter UI
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
    Calendar        Facility       Settings
                       │
                       ▼
                  Rule Manager
                       │
                       ▼
              Reservation Manager
                       │
                       ▼
                  Rule Engine
                       │
                       ▼
               Schedule Generator
                       │
                 ┌─────┴─────┐
                 ▼           ▼
          Conflict Check   Status
                 │           │
                 └─────┬─────┘
                       ▼
                  Repository
                       │
                       ▼
                    SQLite
                       │
                       ▼
              Notification Manager
                       │
                 ┌─────┴─────┐
                 ▼           ▼
              Android       iOS
```

핵심 비즈니스 로직은 UI와 분리한다.

특히 다음 모듈을 독립적으로 구성한다.

- Rule Engine
- Schedule Generator
- Conflict Validator
- Notification Manager
- Repository

---

## 21. 보안 계획

서버가 없으므로 모바일/로컬 보안에 집중한다.

### 적용

- 앱 전용 저장소 사용
- 입력값 검증
- 최소 권한 원칙
- 불필요한 개인정보 저장 금지
- 알림 잠금화면 노출 고려
- Release 빌드 서명
- Debug 로그 제거
- 민감정보 하드코딩 금지
- 데이터 접근 계층 분리
- 날짜 계산 경계조건 테스트

### 저장하지 않는 정보

```text
- 사용자 계정
- 비밀번호
- 학번
- 전화번호
- 외부 예약 사이트 인증정보
```

DB 암호화, 앱 잠금, 생체 인증 등은 현재 MVP 범위에서 제외한다.

---

## 22. 데이터 백업/복원

개인 사용 목적이므로 백업/복원 기능은 구현하지 않는다.

제외:

- 클라우드 백업
- JSON 내보내기
- 데이터 복원
- 계정 기반 동기화

데이터는 해당 기기의 로컬 DB에만 존재한다.

---

## 23. 업데이트

앱 업데이트가 가능하도록 설계한다.

특히 Drift/SQLite의 DB Migration을 사용하여 새 버전에서 데이터 구조가 변경되더라도 기존 일정과 규칙이 유지되도록 한다.

```text
v1 DB
 ↓
앱 업데이트
 ↓
DB Migration
 ↓
기존 데이터 유지
```

앱 업데이트 이후에도 기존 Schedule을 기준으로 알림을 복구/동기화할 수 있도록 한다.

---

## 24. 개발 단계

### Phase 1 — 프로젝트 기반

- Flutter 프로젝트 생성
- Android/iOS 환경 구성
- 기본 테마
- 반응형 레이아웃
- Riverpod 구성
- Drift/SQLite 구성

### Phase 2 — 시설 관리

- Facility 모델
- 기본 강당 생성
- 기본 강의실 생성
- 시설 선택 UI
- 시설 활성/비활성
- 시설 색상

### Phase 3 — 예약 규칙

- Rule 모델
- N일 전 규칙
- 시설별 규칙 연결
- 기본 규칙 제공
- 규칙 관리 UI

### Phase 4 — 일정

- 월간 달력
- 날짜 선택
- 단일 일정 생성
- 예약 스케줄 자동 생성
- 지난 스케줄 처리
- 자동 상태 판정
- 일정 상세 화면

### Phase 5 — 반복 일정

- 시작일
- 종료일
- N일마다 반복
- 특정 요일 반복
- Batch ID 생성
- 여러 Reservation 일괄 생성
- 반복 생성 결과 확인

### Phase 6 — 추가 스케줄

- 사용자 지정 스케줄
- 날짜/시간
- 제목
- 알림 여부

### Phase 7 — 충돌

- 일정 충돌 검증
- 경고 UI
- 일정 추가 차단
- 반복 일정 생성 시 충돌 처리

### Phase 8 — 알림

- 알림 권한
- Local Notification
- Notification ID
- 등록/수정/삭제
- 중복 방지
- 알림 동기화

### Phase 9 — 알림 안정성

별도 작업으로 진행한다.

- Android 실기기 테스트
- iOS 실기기 테스트
- 앱 종료 상태 테스트
- 재부팅 테스트
- 앱 업데이트 테스트
- 알림 권한 변경 테스트
- 배터리 절전 테스트
- 알림 지연/누락 테스트
- 알림 복구 테스트

### Phase 10 — Release

- Android Release APK
- iOS Release 빌드
- DB Migration 확인
- Debug 로그 제거
- 권한 검토
- 최종 기능 테스트

---

## 25. 현재 범위에서 제외되는 기능

```text
- 서버
- 로그인/계정
- 외부 예약 시스템 연동
- 클라우드 동기화
- 데이터 백업/복원
- 주말/공휴일 처리
- 기존 일정에 대한 규칙 변경/재계산
- 사용자 완료 버튼
- 별도 접근성 기능
- 앱 잠금/생체 인증
- DB 암호화
```

---

## 26. 핵심 사용 흐름

### 단일 일정

```text
앱 실행
 ↓
달력에서 날짜 선택
 ↓
시설 선택
 ↓
예약 규칙 적용
 ↓
예약 스케줄 생성
 ↓
추가 스케줄 입력(선택)
 ↓
충돌 검사
 ↓
DB 저장
 ↓
미래 스케줄 알림 등록
```

### 반복 일정

```text
반복 일정 만들기
 ↓
시설 선택
 ↓
시작일 선택
 ↓
종료일 선택
 ↓
N일마다 / 특정 요일 선택
 ↓
여러 Reservation 생성
 ↓
각 Reservation에 규칙 적용
 ↓
각 Schedule 생성
 ↓
충돌 검사
 ↓
DB 저장
 ↓
미래 Schedule 알림 등록
```

---

## 27. 최종 목표

최종 앱은 다음과 같은 사용 경험을 제공한다.

```text
사용자
  ↓
9월 20일 강당 사용을 달력에서 등록
  ↓
앱이 강당 규칙을 확인
  ↓
9월 13일 예약
9월 17일 예약 확인
등의 스케줄 자동 생성
  ↓
9월 13일 00:00
🔔 강당 예약 알림
  ↓
9월 17일 00:00
🔔 예약 확인 알림
  ↓
9월 20일
사용 일정 완료
```

반복 일정의 경우 한 번의 입력으로 여러 사용 일정을 생성하며, 생성 이후에는 각각 독립적인 일정으로 관리한다. 단, `batchId`를 통해 해당 반복 생성 그룹 전체를 한 번에 삭제할 수 있다.

이 프로젝트는 **서버 없는 Local-first 개인용 모바일 앱**을 기본 방향으로 하며, 핵심 가치는 시설별 예약 규칙을 기반으로 사용자가 예약해야 할 시점을 자동으로 계산하고 놓치지 않도록 알려주는 것이다.
